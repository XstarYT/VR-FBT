# Duplicate compatibility directory

The maintained VR-FBT application is one directory above this folder. `Main.py`, `restart.bat`, and `req.txt` here redirect to that canonical copy so older shortcuts continue to work. The old duplicated libraries, models, configuration and executable were removed.
